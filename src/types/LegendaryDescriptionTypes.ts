import { pokemonAllViewTypes } from './pokemonAllViewTypes';
import { pokemonAllSearch } from './pokemonAllSearch';

export type LegendaryDescriptionTypes = {
    legendaryPokes?: pokemonAllSearch[];
    defaultPk: pokemonAllViewTypes;
};